import { DisplayMovieDirective } from './display-movie.directive';

describe('DisplayMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayMovieDirective();
    expect(directive).toBeTruthy();
  });
});
